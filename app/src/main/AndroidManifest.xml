<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.paperplanes.mykamus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".DictionaryApp">
        <activity android:name=".presentation.ui.activity.DictionaryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.ui.activity.AboutActivity"
            android:label="@string/about_action_text"
            android:parentActivityName=".presentation.ui.activity.DictionaryActivity">
        </activity>
    </application>

</manifest>